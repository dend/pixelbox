name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      # TODO: Replace with your build and test commands
      - name: Build and test
        run: echo "Add your build/test commands here"
